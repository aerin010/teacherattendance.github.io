specVersion: "v1beta"
serviceId: "taqi"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "taqi-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
